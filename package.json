{
  "name": "cucumber-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "cucumber-js",
    "bun:test": "bun --bun run cucumber-js",
    "test:tags": "cucumber-js --tags",
    "bun:test:tags": "bun --bun run cucumber-js --tags",
    "lint": "tsc --noEmit && gherkin-lint . && eslint .",
    "generate-model": "node ./scripts/generate-api-models.mjs",
    "check-steps": "export DRY_RUN=true; pnpm test -- --dry-run",
    "bun:check-steps": "export DRY_RUN=true; pnpm bun:test --dry-run",
    "check-steps:usage": "export DRY_RUN=true; pnpm test -- --dry-run --format usage",
    "bun:check-steps:usage": "export DRY_RUN=true; pnpm bun:test --dry-run --format usage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-kms": "^3.474.0",
    "@cucumber/cucumber": "^10.2.1",
    "@types/uuid": "^9.0.8",
    "axios": "^1.6.2",
    "dotenv": "^16.3.1",
    "lodash": "^4.17.21",
    "openapi-fetch": "^0.8.2",
    "swagger-typescript-api": "^13.0.3",
    "ts-node": "^10.9.2",
    "uuid": "^9.0.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@pagopa/eslint-config": "^3.0.0",
    "@types/node": "^20.10.4",
    "gherkin-lint": "^4.2.4",
    "openapi-typescript": "^6.7.3",
    "prettier": "^3.1.1",
    "swagger-typescript-api": "^13.0.3",
    "typescript": "^5.3.3"
  }
}
