import { Then, When } from "@cucumber/cucumber";

When(
  "l'utente revoca l'attributo precedentemente verificato",
  async function () {}
);
Then(
  "si ottiene status code {int} e l'attributo rimane verificato da {string}",
  async function () {}
);
