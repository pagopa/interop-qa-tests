import { Then, When } from "@cucumber/cucumber";

When(
  "l'utente (ri)attiva la finalità per quell'e-service",
  async function () {}
);

Then(
  "si ottiene status code 200 e si verifica che la finalità sia in stato WAITING_FOR_APPROVAL",
  async function () {}
);
