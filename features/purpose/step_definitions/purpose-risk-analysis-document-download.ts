import { Given, When } from "@cucumber/cucumber";

When(
  "l'utente scarica il documento di analisi del rischio",
  async function () {}
);

Given(
  "l'utente ha già aggiornato finalità rispettando le stime di carico per quell'e-service",
  async function () {}
);
