import { Given, When } from "@cucumber/cucumber";

When("l'utente archivia quella finalità", async function () {});

Given(
  "l'utente ha già richiesto l'aggiornamento della stima di carico superando i limiti di quell'e-service",
  async function () {}
);
